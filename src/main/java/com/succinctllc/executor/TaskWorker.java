package com.succinctllc.executor;

public class TaskWorker {
   //if workers run out of work
   //they should broadcast that fact to get work from other nodes
   //that might be backed up in their own queue
   //task worker is backed by executor service
   //should a task worker only work on specific tasks or any task?
   
}
